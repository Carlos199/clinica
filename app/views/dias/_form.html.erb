<%= form_for(dia) do |f| %>
  <% if dia.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dia.errors.count, "error") %> prohibited this dia from being saved:</h2>

      <ul>
      <% dia.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="group">
     
    <%= f.label :nombre, t("activerecord.attributes.dia.nombre", :default => "nombre"), :class => :label %>
    <%= f.text_field :nombre, :class => 'text_field' %>
    <span class="description">Ex: a simple text</span>
  </div>
 
        
<div class="group navform wat-cf">
  <button class="button" type="submit">
    <%= image_tag("web-app-theme/tick.png", :alt => "#{t("web-app-theme.save", :default => "Salvar")}") %> <%= t("web-app-theme.save", :default => "Salvar") %>
  </button>  

 

<% end %>
