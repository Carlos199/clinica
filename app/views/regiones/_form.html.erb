<%= form_for(regione) do |f| %>
  <% if regione.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(regione.errors.count, "error") %> prohibited this regione from being saved:</h2>

      <ul>
      <% regione.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :codigo %>
    <%= f.text_field :codigo %>
  </div>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
